# Database connection settings for FoodFlow backend
# Update these values for your local MySQL server.

# JDBC URL for the database. Use the correct host, port, and schema name.
db.url=jdbc:mysql://localhost:3306/foodflow?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

db.user=root

db.password=Canrod@023

